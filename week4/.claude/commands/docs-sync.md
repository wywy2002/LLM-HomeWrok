同步 week 4 的轻量 API 文档。

步骤：
1. 读取 `backend/app/routers/notes.py` 和 `backend/app/routers/action_items.py`
2. 如果存在 `docs/API.md`，比较当前路由与文档是否一致
3. 将 `docs/API.md` 更新为当前请求/响应结构
4. 返回简短的路由差异总结

规则：
- 仅在 `week4/` 内工作
- 不要虚构代码中不存在的接口
